# Changelog

- fix(security): add express-rate-limit middleware and apply to critical routers
- fix(security): sanitize upload paths to prevent path traversal
- fix(security): move sensitive query parameters from GET to POST body for HR endpoints
