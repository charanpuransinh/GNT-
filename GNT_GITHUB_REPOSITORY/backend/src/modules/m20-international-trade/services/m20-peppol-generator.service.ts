export interface PeppolInvoiceInput{invoice_id:string;issue_date:string;supplier_name:string;buyer_name:string;currency:string;lines:Array<{description:string;quantity:number;unit_price:number;tax_rate:number}>}
const esc=(s:string)=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&apos;");const num=(v:number)=>(Math.round(v*10000)/10000).toFixed(4);
export class M20PeppolGeneratorService{generate(i:PeppolInvoiceInput){if(!i.invoice_id||!i.issue_date||!i.supplier_name||!i.buyer_name)throw new Error("Invoice identity fields are required");const c=i.currency.toUpperCase();if(!/^[A-Z]{3}$/.test(c))throw new Error("currency must be ISO-4217");const lines=i.lines.map((l,n)=>{if(l.quantity<0||l.unit_price<0||l.tax_rate<0)throw new Error(`Invalid invoice line ${n+1}`);return`<cac:InvoiceLine><cbc:ID>${n+1}</cbc:ID><cbc:InvoicedQuantity>${num(l.quantity)}</cbc:InvoicedQuantity><cbc:LineExtensionAmount currencyID="${c}">${num(l.quantity*l.unit_price)}</cbc:LineExtensionAmount><cac:Item><cbc:Name>${esc(l.description)}</cbc:Name></cac:Item><cac:Price><cbc:PriceAmount currencyID="${c}">${num(l.unit_price)}</cbc:PriceAmount></cac:Price></cac:InvoiceLine>`}).join("");return`<?xml version="1.0" encoding="UTF-8"?><Invoice xmlns="urn:oasis:names:specification:ubl:schema:xsd:Invoice-2" xmlns:cbc="urn:oasis:names:specification:ubl:schema:xsd:CommonBasicComponents-2" xmlns:cac="urn:oasis:names:specification:ubl:schema:xsd:CommonAggregateComponents-2"><cbc:ID>${esc(i.invoice_id)}</cbc:ID><cbc:IssueDate>${esc(i.issue_date)}</cbc:IssueDate><cbc:DocumentCurrencyCode>${c}</cbc:DocumentCurrencyCode><cac:AccountingSupplierParty><cac:Party><cac:PartyName><cbc:Name>${esc(i.supplier_name)}</cbc:Name></cac:PartyName></cac:Party></cac:AccountingSupplierParty><cac:AccountingCustomerParty><cac:Party><cac:PartyName><cbc:Name>${esc(i.buyer_name)}</cbc:Name></cac:PartyName></cac:Party></cac:AccountingCustomerParty>${lines}</Invoice>`}}
