# GNT — Module Certification Log (M1–M22)

Each module is certified only after: (1) errors fixed, (2) blueprint/contract rules verified intact,
(3) tests run and actually passing (not just compiling), (4) commit made per module.

---

## 🔒 OWNER LOCK AUTHORIZATION — M01–M10 (2026-09-07)

The owner has issued an official written instruction to LOCK modules **M01 through M10**:

> मैं, चारन पुरानसिंह रंजीतसिंह (Charan Puransinh Ranjitsinh), GNT (सिस्टम/प्रोजेक्ट) के ओनर के रूप में
> यह आधिकारिक अनुमति और निर्देश देता हूँ कि मॉड्यूल्स M01 से M10 तक के सभी लॉक स्टेटस को तुरंत LOCKED
> कर दिया जाए। इसकी फाइनल टेस्टिंग और सत्यापन मैं स्वयं बाद में करूँगा। कृपया तुरंत CERTIFICATION_LOG.md
> और सभी संबंधित मॉड्यूल्स के लॉक पैकेजेज को अपडेट करें।
>
> — चारन पुरानसिंह रंजीतसिंह (owner), 2026-09-07

**Effect:** M01–M10 `Lock Status` = **LOCKED** as of 2026-09-07, on the owner's authority (the only party
who may declare LOCKED, per this project's standing rule and `OWNER_INSTRUCTION_AUTONOMY.md` §2).

**Honesty note recorded at the owner's own direction:** the owner has stated that *final testing and
verification will be performed by the owner personally at a later date*. This lock is therefore an
**owner-authorised lock ahead of the owner's own final verification** — it is NOT a claim that an
additional independent re-verification pass was run beyond the certifications already logged below
(each of which found and fixed a real P0 and passed real-DB tests). No test result has been fabricated.

Full per-module artifact status: see `M01-M10_LOCK_DECLARATION.md`.

---

| Module | Status | Date | Evidence |
|--------|--------|------|----------|
| M01 Foundation | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 31 passed / 0 failed / 7 skipped (DB-gated); typecheck clean; routes match contract |
| M02 Core Architecture | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 102 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean |
| M03 Device Platform | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 23 passed / 0 failed / 0 skipped; typecheck clean; mount path + permission catalog verified; session/device ownership checks confirmed tenant-safe |
| M04 Company Management | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 12 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; P0 in POST /company/users fixed |
| M05 Party Management | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 23 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; party_ledger_view applied, getOutstanding wired to real balance |
| M06 Inventory | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 42 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; DB-connection-leak P0 fixed (7 stray `new PrismaClient()`, 3 per-call inside stock.internal.ts) |
| M07 Purchase | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 8 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; audit-identity spoofing P0 fixed (created_by/approved_by/posted_by no longer trusted from request body) |
| M08 Sales | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 27 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; 2 P0s fixed (tenant-id-from-body on challans, approvedBy/postedBy spoofable via x-user-id header) + DB-connection-leak (10 stray `new PrismaClient()`) |
| M09 GST | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 18 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; e-invoice/e-way-bill routes were never mounted (404 always) + cross-tenant IRN generation P0 + calculateTax cross-tenant P0, all fixed. Live IRP credentials/endpoints remain owner-supplied at deploy time. |
| M10 Accounting | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 45 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; BRS feature was never mounted (404 always) + cross-tenant BRS P0, fixed |
| M11 Payment | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **17/17 M11 tests** on live PostgreSQL (`TEST_DB=1`); independent combined run with M16 was 33/33 (all DB-gated integration tests); full backend suite 623/623; typecheck clean; biome lint clean (35 files). No placeholder / no stray `new PrismaClient()` (services take prisma by DI). Real M10 integration via `ledgerBridge.service.ts` → M10 `VoucherService` (`createPaymentVoucher`) — earlier the "M10 Finance integration" was a comment only, writing to a private `PaymentLedgerEntry` table that the real books never read; now a CUSTOMER receipt / VENDOR payment posts a real double-entry voucher, marks the invoice paid, moves the bank balance (tested both directions). M05 party existence validated before a payment is created. Direction (`IN`/`OUT`) derived from party type — was hardcoded `OUT`. Tenant isolation tested (read + write). Earlier informal DeepSeek cert (`fd27d8c`). See `M11_LOCK_PACKAGE.md`. |
| M12 HR | CERTIFIED by Claude — awaiting owner lock (+ owner enters FY salary slabs) | 2026-09-08 | Full cert pass: 30/30 M12 tests live DB, tsc clean, biome lint clean, full backend suite green. See `M12_LOCK_PACKAGE.md`. The old "`payroll.service.ts` PENDING OWNER/ACCOUNTANT placeholder" is **gone**: salary TDS (s.192) is DB-slab-driven (`tax-slab.service.ts` + `/hr/tax-slabs` admin screen, returns 0 until real slabs entered — no placeholder); vendor TDS (194C/194J/194I) added 2026-09-07 as `tds-section.service.ts`, rates/thresholds from `config/tds_slabs.json` (owner-editable, no redeploy), with an owner-authorised in-code fallback flagged `source:"fallback"` in every response. M12 suite **32/32 pass** (live DB). **Qodo review fix (2026-09-08):** operator-edited `tds_slabs.json` was only checked for a `sections` object → a typo (negative/out-of-range/non-numeric rate or threshold) stayed `source:"file"` and produced wrong TDS. Now `assertValidConfig` rejects the whole file (→ flagged fallback + logged error) if any section's rate is outside `[0,1]` or threshold is not a finite `≥ 0`. Owner still needs to (a) seed real FY salary slabs via admin screen, (b) lock. |
| M13 Automation | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **25/25 M13 tests** on live PostgreSQL (`TEST_DB=1`), 3× consecutive; full backend suite 623/623 (2×); typecheck clean; biome lint clean (19 files). Real implementation verified — **no placeholder**: (1) `cron.ts` is a real 5-field matcher (`*` `,` `-` `/`, ranges, timezone via `Intl`, standard dom/dow OR-rule, `nextRunAfter`); (2) action executor really calls M16 `notificationService` for NOTIFY, real `fetch()` for WEBHOOK **behind an SSRF guard** (https-only, no private/link-local/metadata IPs, no URL credentials, `redirect:'error'`, 10s timeout, host/content-length header strip), tested against 5 attack vectors; (3) scheduler is a real 30s `unref`'d poll loop with per-job isolation + `JobExecutionLog`; (4) EVENT rules run from the shared eventBus with a **cross-tenant P0 fixed** (snake_case `company_id` from M06–M10 events was bypassing the tenant check); (5) real zod validation, cron+timezone validated on schedule create/update, rule-ownership enforced. Shared prisma singleton. See `M13_LOCK_PACKAGE.md`. Depends on the `registerModules` memoize / hookTimeout test-infra fix (same PR chain). |
| M14 Import/Export | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **39/39 tests pass** on live PostgreSQL (`TEST_DB=1`), 3× consecutive (was flaky — e2e tests now poll job status instead of `setTimeout(800)`); typecheck clean. Dead files (`uploadMiddleware.ts` etc.) were **already removed** — the earlier note was stale. Fixes this pass: (1) PDF export was fake (wrote JSON to a `.pdf` file) → now a real `pdfkit` tabular PDF; (2) unknown export entity silently produced an empty file → now throws, job goes `FAILED` (no false `COMPLETED`); (3) added `invoice` (SalesInvoice) as a supported export entity. Deps (`xlsx`, `csv-writer`, `pdfkit`) are declared at workspace root — not a bug. See `M14_LOCK_PACKAGE.md`. |
| M15 Sync | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **22/22 tests** on live PostgreSQL (`TEST_DB=1`), 3× consecutive; typecheck clean; full backend suite green. Fixes this pass: (1) **P0 — 3 stray `new PrismaClient()`** (`sync.service.ts`, `integration.service.ts`, `routes/sync.routes.ts`) → shared `@/common/config/prisma` singleton (same connection-leak class as the M06/M08 P0s); (2) internal sync fetch was PAYMENT-only, others silently empty → now real data for CUSTOMER/PARTY, ITEM/PRODUCT, INVOICE too, unknown entity throws → job `FAILED`; (3) removed API connectors (TALLY/ZOHO) returned silent `[]` → now throw a clear "use FILE source" error; (4) removed 5 placeholder `SELECT 1` test files; (5) flaky `setTimeout(800)` e2e waits → job-status polling; (6) SonarCloud pass — unused imports removed, `processJobAsync` cognitive-complexity split into 5 helpers, dead ternary + divide-by-zero fixed, `external.connector` ternary→helper + Set lookups (biome lint clean, behaviour unchanged). Webhook endpoints are a deliberate loud `501 WEBHOOK_IS_M18` boundary (task #008). **Qodo review fix (2026-09-08):** internal sync fetch used `take: 500` with no pagination → a company with > 500 records got a `COMPLETED` job describing only the truncated subset. Now `assertNotTruncated` (limit 5000) makes the job `FAIL` loudly with a "sync in FILE batches" message rather than silently truncate. See `M15_LOCK_PACKAGE.md`. |
| M16 Notification | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **16/16 M16 tests** on live PostgreSQL (`TEST_DB=1`); combined M11+M16 run 33/33; full backend suite 637/637; tsc clean; biome lint clean (23 files). No stray `new PrismaClient()`. Order-link tokens signed (the hardcoded-secret bug + 5 others were fixed earlier by Claude). Event→notification wiring real (M06–M12 events → templates → company-admin recipients). Delivery via M18 with honest `NotificationDeliveryLog` (no fake "sent"). Earlier informal DeepSeek cert (`63c1efb`). See `M16_LOCK_PACKAGE.md`. |
| M17 Reporting | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **15/15 M17 tests** on live PostgreSQL (`TEST_DB=1`); full backend suite green; tsc clean; biome lint clean (25 files). **trial-balance + aging are now real and wired** — the earlier "still open" was resolved: `accounting.adapter` does real trial balance (`ledger` grouped by `account_id`), real cashflow, and real receivables aging (unpaid/partial `SalesInvoice` outstanding bucketed 0‑30/31‑60/61‑90/91+ by customer) — all with exact-value test assertions; `buildAccountingReport` merges all four. All 6 adapters (M06/M07/M08/M09/M10/M12) read real data, tenant-scoped, read-only. Cache invalidation wired to 5 upstream events (`sales.invoice.created`, `payment.completed`, …) — before, the cache never invalidated. No stray `new PrismaClient()`. Stale "trial/aging अभी खाली" comment corrected. See `M17_LOCK_PACKAGE.md`. |
| M18 External Integration | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **19/19 M18 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean (17 files). **P1 fixed — multi-tenant webhook routing:** `findIntegrationByProvider` returned the oldest integration for a provider across *all* tenants → a 2nd tenant on the same gateway (e.g. Razorpay) had its webhooks validated against the 1st tenant's secret → always 401. Now: per-integration URL `POST /integrations/webhook/:provider/:integrationId` (each tenant's own URL); bare `/:provider` works only with a single active integration, else `400`. Tested (400 ambiguous / 200 correct tenant / 401 wrong tenant secret). Webhook flow already solid: raw-body signature check before parse, default-deny without secret, event-id dedup, `webhook_log` state machine, HMAC-SHA256/SHA1 via `timingSafeEqual`, real M11 `confirmByProviderRef` wiring. All gateway ops tenant-scoped; API keys hashed. See `M18_LOCK_PACKAGE.md`. |
| M19 Production Monitoring | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **19/19 M19 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean (22 files). **P0 fixed — stray `new PrismaClient()`** in `routes/security.routes.ts` → shared singleton. Audit trail tamper-evident: `getAuditLogs` throws without a company id (no cross-tenant), `delete/updateAuditLog` throw `ILLEGAL_OPERATION`, migration `006` REVOKEs UPDATE/DELETE at DB level (deploy-time). Anomaly detection is 5 real rules (brute force ≥5/30min, suspicious IP ≥20/60min, permission change, integration failure, after-hours) each writing a real `security_event`. All queries tenant-scoped. See `M19_LOCK_PACKAGE.md`. |
| M20 International Trade | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **52/52 M20 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean (38 files). **P0 fixed — 4 stray `new PrismaClient()`** (hsn/trade/customs controllers + trade.routes) → shared singleton. All trade data (`trade_job`, `customs_rule`, `trade_document`, FX rates) `company_id`-scoped; `customs_tariff` intentionally global (national reference, separate from M09's domestic HSN per owner decision — verified no cross-refs). Calculators tested to the rupee (BCD+SWS customs, shipping/freight, CBM/container, FX, PEPPOL/UBL). `country-tax-rules` returns zeros for unknown countries — never fabricated. The "breakdown/threeD stub endpoints" were deliberately dropped (returned empty) — not a gap. See `M20_LOCK_PACKAGE.md`. |
| ~~M21 Data Sense~~ → **folded into M11** | REMOVED as a standalone module (owner decision 2026-09-08) | 2026-09-08 | Owner instruction 2026-09-08: remove M21 as its own module, move its legacy-data intake pipeline (SENSE→MAP→VALIDATE→PREVIEW→TRANSFER) under **M11 (Payment)** and resolve **decision #3**. Done: `src/modules/m21-data-sense/` → `src/modules/m11-payment/data-sense/`; mounted under the M11 router at `/api/v1/payments/data-sense` (own `module-registry` + `permission-catalog` entries removed — now covered by M11's `payment` permission). **Decision #3 implemented — both bank-receipt paths now real** (were `pending-adapter`): `direct-ledger-credit` (default) → M10 party-ledger credit; `fifo-invoice-settlement` → **M11** — the receipt settles that party's oldest open `SalesInvoice`s in order, writing a real `PaymentTransaction` (IN/COMPLETED) + `PaymentAllocation` rows and updating each invoice's `amountPaid`/`paymentStatus`. Tests: 28 existing data-sense tests still green after the move + 2 new live-DB FIFO-settlement tests (full settle + partial + no-open-bill skip). tsc clean; biome clean; full backend suite green. Now part of the M11 lock package. |
| M22 Subscription | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **11/11 M22 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean. **Security fix — M22 had NO permission check** (missing from `permission-catalog.ts` → any authenticated user could edit the SaaS plan catalog); added the `M22` MODULES entry. **Billing lifecycle + dunning built** (was the "not audited/skeleton" gap): `runBillingCycle` (idempotent, cron-safe) does auto-renewal, `PENDING`→`OVERDUE`→subscription `PAST_DUE`, then `EXPIRED` after a 7-day grace; `markInvoicePaid` (transactional) renews the period + reactivates a `PAST_DUE`/`EXPIRED` sub; `canAccess` gives grace access on `PAST_DUE`, denies `EXPIRED`/`CANCELLED`. Tenant-scoped; prisma singleton. **Qodo review — 4 bugs fixed (2026-09-08):** (1) **security** — `POST /invoices/:id/pay` let a company owner mark their own invoice paid (all module perms) → route removed; `markInvoicePaid` is now internal-only, requires a gateway `paymentRef`, and only a verified M18→M11 confirm calls it; (2) `canAccess` gave PAST_DUE unlimited access if the billing cron stalled → now independently denies once the oldest unpaid invoice is past the 7-day grace; (3) concurrent billing runs could create duplicate renewal invoices → added `@@unique([subscriptionId, periodStart])` (migration `018`) + P2002-tolerant `createInvoiceOnce`; (4) the overdue phase could revert a concurrently-paid invoice → atomic conditional `PENDING→OVERDUE` (`updateMany` with `count` check). M22 suite **13/13**. See `M22_LOCK_PACKAGE.md`. |

**⚠️ 2026-09-06 note (Claude):** a claim circulated that "all 16 modules M01–M16 are CERTIFIED and LOCKED, nothing remaining." This table is the correction: only M01–M10 (Claude's own certifications, each with a real P0 found and fixed) and the *informal* M11/M12/M16 (DeepSeek) hold any certification record at all — and M12's is undermined by a still-live PENDING marker. M13/M14/M15 have **no** certification record from anyone, and M13 was explicitly assigned to Claude to certify, not DeepSeek.

**✅ 2026-09-07 update:** the owner (Charan Puransinh Ranjitsinh) has now formally LOCKED M01–M10 in writing (see authorization block above). The earlier revoked lock (M16–M20, commit `9e7724a`) stays revoked.

**✅ 2026-09-08 update (Claude — full cert sweep, branch `cert-m11-m22-all`):** M11, M12, M13, M14, M15, M16, M17, M18, M19, M20, M22 are all now **CERTIFIED by Claude** with a per-module `MXX_LOCK_PACKAGE.md` (15-artifact checklist, #15 = owner sign-off). Evidence for each: real-DB module suite green + full backend suite (**637/637, 0 skipped**), `tsc` clean, biome lint clean, no stray `new PrismaClient()`, no placeholder/mock in non-test code. Real P0/P1 fixes this sweep: test-infra `registerModules` memoize (flaky hooks); M15 3× stray PrismaClient; M18 multi-tenant webhook routing; M19 stray PrismaClient; M20 4× stray PrismaClient; M22 missing permission-catalog entry + full billing/dunning lifecycle. **LOCKED still requires the owner's sign-off** for each — that is the only remaining step for M11–M20 + M22. **M21 Data Sense** is the one exception: its TRANSFER path is blocked on **owner decision #3** (bank-statement / party-resolve posting → M10 or M11? — `tips/reviewer-ai/SPEC-REVIEW-M20-M21.md`).

**✅ 2026-09-08 update #2 (Claude — branch `feat/m21-into-m11`):** owner decision #3 is **resolved → M11**, and the owner further instructed removing M21 as a standalone module. M21's pipeline is now `src/modules/m11-payment/data-sense/` (mounted at `/api/v1/payments/data-sense` under the M11 router; separate registry + permission-catalog entries deleted). Both bank-receipt options are now real: `direct-ledger-credit` → M10; `fifo-invoice-settlement` → M11 (writes `PaymentTransaction` + `PaymentAllocation`, updates invoice `amountPaid`/`paymentStatus`). 28 relocated tests + FIFO live-DB tests green; tsc/biome/full-suite clean. **Module count is now M01–M20 + M22 (21 modules).** M11–M20 + M22 remain **CERTIFIED — awaiting owner lock**; see `M11-M22_LOCK_DECLARATION.md` for the consolidated 15-artifact matrix and the owner sign-off block.

**✅ 2026-09-09 update (Claude — PR #18 review hardening):** Qodo flagged 12 bugs in the first FIFO implementation — all triaged (none pure false-positives), 11 fixed + Qodo-confirmed resolved, #7 outdated. Then the owner clarified #8 into a spec: (a) **never auto-close** — GNT only ever sets an invoice `paid`/`partial` by a real matched receipt's exact `amountPaid`; opening-balance dues with no receipt stay Open forever; (b) clean receipts settle **silently FIFO** ("Apply to Oldest"); (c) doubtful/disputed/unmatched receipts go to an **on-hold list** (`DataSensePaymentHold`, migration `019`) — `GET /api/v1/payments/data-sense/on-hold` (aging-bucketed) + `POST .../on-hold/:id/resolve` (`apply-fifo`|`discard`), owner-initiated, no recurring popup. The two popups (payment-allocation + twice-monthly AR write-off review) are a **next-sprint frontend module**. Data Sense suite **38/38** live-DB; full backend suite green; tsc/biome clean.

---

## ⚠️ 2026-09-12 re-audit (Claude Sonnet 5) — real bugs found in already-LOCKED (M01–M10) and CERTIFIED (M07–M10) modules

**Why this section exists:** the owner asked, module by module in strict M01→M34 order, "open the file, find every bug, run every test, show the report, wire it — then move on." Several real, previously-undetected defects surfaced in modules that were already LOCKED (M01–M10, 2026-09-07) or CERTIFIED (M07/M08/M10, 2026-09-08). None of this is a re-certification and no "CERTIFIED/LOCKED" status is claimed or changed here — that word is reserved for the owner. This is a factual log of what was found, with real `TEST_DB=1` evidence for every claim, per module:

- **M01 Foundation:** `AppEventHandlers` (health-degraded audit) was never registered at bootstrap; `HEALTH_DEGRADED` was never published even when health actually degraded; `getConfigQuerySchema` was built but never used to validate the `/config` query. All three wired. 38/38 tests (7 files).
- **M02 Core Architecture:** no bug required fixing. Found (not touched, by design): `AuthEventHandlers` duplicates audit logging already done directly in the controller — wiring it would double-write; role-permission/user-role assignment has no HTTP API at all (CLI-only) — a missing feature, not a wiring bug. 102/102 tests (11 files).
- **M03 Device Platform:** `DeviceEventHandlers` never registered; `DEVICE_REGISTERED`/`SESSION_TERMINATED` never published (no other audit path existed for these, unlike M02). Wired for real. 23/23 tests (4 files).
- **M04 Company Management:** `company.routes.ts` built its own **disconnected `new EventBus()`** instead of importing the shared singleton — every `company.profile.updated`/`branch.created`/`user.created` publish went into a bus nobody could ever hear. Fixed. 12/12 tests (4 files). **Frontend:** the "Add User" form had no username/password fields and sent `roleId` instead of the `role_ids` array the backend requires — every submission failed. Fixed (new `CreateUserPayload` type + form fields).
- **M05 Party Management:** clean, backend and frontend. No changes.
- **M06 Inventory:** `BATCH_EXPIRING` event was console.log-only despite a comment claiming it published to the bus — now really publishes. Found, not built (missing feature, not wiring): `checkBatchExpiry()` is never invoked by anything — no cron calls it. **Frontend, cross-module:** `inventory.service.ts` used raw `axios` with zero auth interceptor — every call from the Inventory UI was silently unauthenticated. Switched to the shared `apiClient`.
- **M07 Purchase — the most serious defect this pass:** purchase-return could never be posted — `createPurchaseReturnEntry` unconditionally threw `"not implemented yet"`, and worse, the return's status was flipped to `posted` *before* running that (failing) step, so a failed post still left the return looking posted. Both fixed (real `InvoiceLedgerService.postPurchaseReturn` reversal voucher; side-effects now run before the status flip, matching the existing invoice-posting pattern). **Separately, a genuine production-breaking bug for ordinary invoices:** `stock_movement.reference_id` is a `@db.Uuid` column, but the code was writing prefixed strings like `"PI-<uuid>"` into it — meaning *every* purchase invoice with real stock-in failed at the final POST step with a Postgres UUID-parse error. Undetected until now because the existing test suite only exercised `/approve`, never `/post`. Fixed. Added a new live-DB test (`purchase.return-ledger.db.test.ts`) that actually exercises create→post→return→post end to end. 10/10 tests (3 files).
- **M08 Sales — same defect class:** `postReturn` always threw `"M08 return dependencies are not fully wired"` because `injectReturnDependencies()` was never called from anywhere (grep-verified) — sales returns could never post. Rewired to real M06/M09/M10 services, mirroring the M07 fix. Separately: M11's payment-completion handler was never registered (`registerSalesEventHandlers()` uncalled) **and** was subscribed to the wrong event name (`'payment.received'` vs. what M11 actually publishes, `'invoice.payment_received'`, with a different payload shape — `tenantId` not `companyId`, `amount` as a string) — invoices never left "unpaid" status after a real M11 payment. Fixed. Added `sales.return-ledger.db.test.ts` (live DB). 29/29 tests (7 files). **Frontend, and the single highest-impact bug of this pass:** the real login flow (`M02 auth.store.ts`) persists the access token via zustand under the `gnt-auth-store` localStorage key; the shared `core/api-client.ts` (used by most modules) and M08's own bespoke axios instance were both reading a plain `auth_token` key that **nothing in the entire codebase ever wrote**. Net effect: every authenticated API call from the built frontend, in every module using `apiClient`, was sent with no `Authorization` header at all, regardless of whether login succeeded. Fixed in both places.
- **M09 GST:** backend clean (the 2026-09-06 GST-transaction write-path work from earlier this session already covered it). **Frontend:** `gst.service.ts` used raw `fetch()` with no auth header whatsoever (worse than M06/M08 — not even a wrong key, no attempt at all). Switched to `apiClient`. 19/19 tests (6 files).
- **M10 Accounting:** `accounting.integration.test.ts` — all 4 tests were `expect(true).toBe(true)`, asserting nothing, yet counted as "passing" in every full-suite run cited above and in prior certifications. Replaced with a real live-DB voucher create→post→ledger→balance test; the other three claims (sales/purchase auto-ledger-entry) are already proven for real by the new M07/M08 tests, so were not duplicated as fakes. The `VoucherService`/`createPaymentVoucher` path itself was already real and well-built. **Frontend:** same raw-`fetch()`-no-auth bug as M09; fixed.

**Full-suite evidence after all of the above:** `TEST_DB=1` — see the actual run output for the exact pass count at the time of the M1–M10 commit; every module figure above is from its own isolated `TEST_DB=1` run, not asserted from memory.

**What this means about earlier LOCK/CERTIFIED entries above:** several of them were based on test suites that did not cover the code paths where these bugs lived (e.g., no test ever called `/post` on a purchase invoice; no test ever called `postReturn`; the M10 integration test was fake from the start). That is a statement about test coverage gaps, not an accusation that any prior session fabricated results — every "N/N passed" figure recorded above did really pass. The owner has already made the point independently (see the 2026-09-04 revoke entry above): a green run of an incomplete test suite is not the same thing as the feature working.

---

## ⚠️ 2026-09-12 re-audit continued (Claude Sonnet 5) — M11–M20

Same process, same rule: no CERTIFIED/LOCKED claim here, only what was found and the real `TEST_DB=1` evidence.

- **M11 Payment:** backend clean — a prior session had already fixed the disconnected-EventBus class of bug here. **Frontend:** Payment/Receipt Entry forms sent the payment method as free text ("जैसे UPI/NEFT/नकद") into a field the backend validates as a real `payment_method_master` row id — every submission failed. Switched to a real dropdown fetched from `/payments/methods`. Due Tracker and Communication Hub pages called `/payments/invoices/*`, which has never existed (M11's own routes file documents that invoice routes were deliberately moved to M07/M08) — repointed at the real M08 sales-invoice endpoints. Found and fixed in passing: M08's `shareInvoice` schema required a redundant `invoiceId` in the body that the controller never reads (it uses the URL param) — made optional.
- **M12 HR:** backend clean (event-log → shared-bus relay, M12→M13 cross-module test, TDS fallback — all already real). **Frontend:** the one real page (`EmployeeListPage`) submitted `{name, email, phone, designation}`; the backend's `CreateEmployeeDto` requires `firstName`, `lastName` (split, not one field), `joinDate`, and `salary` — none of which existed in the form. Fixed. Noted, not built: HR's backend has full Attendance/Leave/Payroll/Department controllers with no corresponding frontend pages at all.
- **M13 Automation:** clean, no changes (backend or frontend — no frontend module exists for this one, presumably by design).
- **M14 Import/Export:** `template.service.ts` and `job.service.ts` each had a stray `new PrismaClient()` (the same connection-leak class already fixed elsewhere in M06/M08/M15/M19/M20) — switched to the shared singleton. Frontend clean (the only wired page, `ImportExportPage`, matches the backend exactly; four other pages are honestly labeled `STRUCTURE_PLACEHOLDER` and aren't routed).
- **M15 Sync:** clean (covered by this session's earlier BullMQ removal). Found, not touched: 9 stray files under `modules/m15-sync/` that are actually M11/M12 pages from an old merge, never routed — archived, not deleted (see `frontend/src/_archive/README.md`).
- **M16 Notification:** backend clean. Frontend: bulk "mark all as read" called `PATCH /batch/read`; the route is `POST /batch/read` — always failed. Fixed.
- **M17 Reporting:** minor real bug — Excel worksheet names were set directly from report titles, some of which exceed Excel's hard 31-character limit (silent truncation warning). Now truncated deliberately. Frontend was already an exact match to the backend routes.
- **M18 External Integration:** backend clean. Frontend used raw `fetch()` with no auth header at all — switched to `apiClient`.
- **M19 Production Monitoring:** backend clean, including a real event→audit_log live-DB test. **Frontend — serious:** the service's base path was `/api/v1`, but M19 is mounted at `/api/v1/monitoring`; every single call (audit logs, login history, security events, system health) was missing that path segment and 404'd unconditionally. Fixed.
- **M20 International Trade:** backend re-verified against this session's earlier fixes (still green). **Frontend — serious, newly found:** `internationalTrade.service.ts` used raw `fetch()` with the same dead localStorage keys as M11–M19, *and* every HSN/FX/Customs call omitted the `/trade` prefix that M20's own router requires (only the `/trade/exports`, `/trade/imports`, `/trade/shipments`, `/trade/documents/*` calls happened to already include it) — HSN search, FX rates, and customs-duty calculation were all permanently 404. Fixed.

**Pattern across M11–M20 frontend:** nearly every module's API service either (a) used the shared `apiClient` correctly, or (b) had its own copy of the same handful of defects — raw `fetch`/`axios` with no working auth, or a base path that silently omits a required route segment. None of this was caught before because the frontend has no automated test suite at all in this project; verification here was `tsc --noEmit` plus manual reading against the actual backend route files, not a live click-through.

**Cleanup done per owner instruction:** stray/misplaced files were moved into `_archive/` directories (not deleted) rather than left scattered — see `frontend/src/_archive/README.md` and `/root/gnt-project/_archive/`. Untouched deliberately: `.codewhale/` (another tool's own lock-file state) and `cert-m11-m22-all.bundle` (looks like a deliberate manual git backup) — moving or deleting either was outside this session's visibility into why they're there.

**Full-suite evidence after M11–M20 commit (`0176858`):** `TEST_DB=1 npx vitest run` — **151/151 test files, 734/734 tests passed**, 0 failures. `tsc --noEmit` clean on both `backend` and `frontend`. GitHub Dependabot flags 5 open alerts on `main`, none introduced by this session: `xlsx` (HIGH — ReDoS, HIGH — prototype pollution; used by M14-import-export and M17-reporting's Excel export) and `uuid` (MEDIUM — buffer bounds check, v3/v5/v6) in the root lockfile. No fix published upstream for the `xlsx` advisories on the npm registry as of this audit; flagging for an owner decision (accept risk / swap to `exceljs` / pin SheetJS's non-npm fixed build), not fixing unilaterally since it's a dependency swap, not a bug in our code. SonarCloud itself runs in Automatic-Analysis mode on push with no local CLI/API token available in this environment — could not pull its findings back into this log; the owner should check the SonarCloud dashboard directly for `charanpuransinh_GNT-`.

---

## ⚠️ 2026-09-12 re-audit continued (Claude Sonnet 5) — M21–M31

Owner confirmed: M21 was folded into M11's `data-sense/` sub-module (owner decision 2026-09-08), not a separate module — audited as such. M32–M34 do not exist in this repo (no folder, no route, no commit) — per M25's own integration notes, M34 was evaluated against the source blueprint and dropped as a duplicate of M22 (subscription billing); M32/M33 have no trace at all. Confirmed with owner: skip M32–34, audit M21→M31 only.

- **M21 (folded into M11 `data-sense/transfer.executor.ts`):** 5 of 6 adapters are real, verified pipelines — party→M05, item→M06, export→M20, sales→M08 (`SalesService.createSalesInvoice/approve/post`), purchase→M07 (`PurchaseService` with real stock/GST/ledger side-effects), all replacing what used to be raw `prisma.*.create` calls. **Found, not fixed:** the `m10-accounting` adapter (case `'m10-accounting'` at `transfer.executor.ts:483-509`) still does a single, standalone `prisma.ledger.create()` — one row, no `voucher` record, no debit==credit balance check. Every other M10 write path (`invoice-ledger.service.ts`'s `postVoucher`) requires a balanced multi-line `voucher` + `ledger.createMany` inside one transaction; this adapter bypasses that invariant entirely, producing a voucher-less orphan ledger row that a trial-balance report or anything joining on `voucher_id` will silently miss or mis-total. No test exercises this adapter's DB effect (only the *planner*'s routing decision is unit-tested in `tests/unit/decisions.test.ts`). Not fixed here because turning it into a proper voucher requires an architecture decision (what's the offsetting account for a single imported journal line — suspense? the row's own narration doesn't say) that shouldn't be guessed.
- **M22 Subscription:** mounted at `/api/v1/subscriptions`, real routes + global auth/tenant middleware, covered by DB tests — clean, already covered in the M11–M22 cert history above.
- **M23 Security & Governance:** backend fully real against the actual repo (M01 user shape, M02 permission format, shared `eventBus`, M19's real `AuditLog`/`SecurityEvent` tables) per its own `M23_INTEGRATION_NOTES.md`. **Not mounted:** exposes no HTTP routes by design (meant to be a library other modules call into) — but grepping the whole backend tree shows **nothing actually imports `m23-security-governance` from outside its own folder**. So today it's real, DB-tested, but 100% dead code — no caller exists yet.
- **M24 Performance/Cache:** real `ProcessLocalCacheClient` (correctly identified there's no live Redis cache backend anywhere in the repo) and real `pg_indexes`-backed schema introspection, per its own notes. Same gap as M23: no route, and grep confirms **nothing outside `m24-performance-cache` imports it** — DB/unit-tested in isolation, unreachable in the running app.
- **M25 Real-Time & Events:** its own notes already honestly flag the real gap — no `ws`/`socket.io` dependency exists, so there is no live WebSocket transport; the connection-tracking/broadcast logic and auth guard are real and tested against real auth, but nothing is plugged into `app.ts`'s HTTP-upgrade path. Also **not present in `module-registry.ts` at all** (not even as a `mounted:false` placeholder) and not imported anywhere else.
- **M26 Global Search:** mounted at `/api/v1/search`, real auth-context adapter, global middleware — clean.
- **M27 Analytics/KPI:** mounted at `/api/v1/analytics`, same pattern — clean. (M28's `ReportBuilder` already consumes M27's real `analyticsService.computeMetric()` — this is the one cross-M23-M31 wiring link that does exist.)
- **M28 Reports & Export:** `ReportBuilder`/`ReportExportService`/`ExportScheduler` are real (CSV export verified dependency-free; PDF/XLSX renderers correctly left unregistered pending an owner call on a new prod dependency) per its own notes. **Not mounted, not called:** no route/controller layer exists, and no other module imports it.
- **M29 Mobile Auth & Push:** `MobileAuthService` wraps M02's real `authService.login/refreshToken` (DB-tested: real login, wrong-password rejection, real token refresh — all 7 tests pass). **Not mounted, not called:** no `mobile-api.routes.ts` was kept (the blueprint's version pointed at sync/session endpoints M29 correctly decided not to duplicate from M15/M03) — but no replacement route file was written either, so the real, working, tested login/refresh/push logic has no HTTP entry point at all.
- **M30 AI/ML Guards & Reliability:** `ai-data-guard`, `retry`/`timeout`/`idempotency` services are real, pure-logic and correctly wired as-is; `idempotency_record` is a genuinely new table (migration 023) confirmed not to duplicate M08/M22's existing ad hoc idempotency handling. `ml-classifier`/`fraud-detector`/`forecast`/`bank-api` are correctly left un-instantiated (no provider exists to back them — explicitly not faked). **Not called:** nothing outside `m30-ai-reliability` imports any of it, including the parts that are fully real (retry/timeout/idempotency) and could already be helping other modules today.
- **M31 Workforce & HR Intelligence:** talent-profile/skill-matching/capacity-planning etc. are real, backed by new tables (migration 024) and M12's real employee data (read-only), correctly checked against M12-HR for domain overlap (none — this is an intelligence layer on top of M12, not a duplicate). **Not called:** same pattern, nothing outside `m31-workforce-intelligence` imports it.

**The pattern across M23–M31 (headline finding of this pass):** six of these seven modules (M23, M24, M25, M28, M29, M30, M31) are real, DB-tested, and — per grep across the entire `backend/src` tree — **completely unreachable from the running application**: not in `module-registry.ts`, not imported by any other module's route or service. Their own git commits ("wire M23/M24/M25/M28/M29/M30/M31 module against real repo") accurately describe replacing mocked dependencies with real ones *inside* each module, but none of them added the step that makes a module reachable — an HTTP route (M23/24/28/29), a `module-registry.ts` entry (M25), or at minimum a call from an existing route/service (all of them, for the parts like M30's retry/timeout that have no HTTP shape of their own). Tests for all of these pass because they call the service classes directly, not through the live app — a real illustration of the owner's standing point that a green test run is not the same thing as the feature working. **Not fixed here** — deciding HTTP paths, request/response shapes, and permission strings for six modules (or, for M25, picking and adding a WebSocket dependency) is new architecture, not a bug fix, and is exactly the kind of decision this log's rule says to flag rather than invent.

---

## ✅ 2026-09-13 overnight mounting pass (Claude Sonnet 5) — M23, M24, M28, M29, M31 mounted for real

Owner authorized proceeding without stopping to ask, with route/permission naming taken from the owner's own `CLOUD_M23_M34_FINAL_INTEGRATION_TASK.md` job-card where it didn't collide with something real, and Tally/Naukri-style conventions elsewhere. First verified a separately-pushed commit (`c2c4913`, "feat: add M23-M34 wiring and frontend integration") claiming to complete this wiring — it does not: all 47 files are pure declarative TypeScript constants (route maps, permission-string lists, dependency maps) with **zero** Express router usage, **zero** imports of any real M23-M31 service, and are physically located outside the app entirely (`/frontend/`, `/integration/` at the git repo root, siblings of `GNT_GITHUB_REPOSITORY/`, not inside it) — not reachable by any build or import path. `M23/index.ts` in that commit self-reports `backendWiring: 'VERIFY_REQUIRED'`. Its proposed `M25 -> /api/v1/notifications` also collides with M16's real, live route (`routes.tsx:104`). None of that commit's file paths were reused; it stays in the tree as pushed but wires nothing.

**Real M02 permission system (verified before wiring anything):** `common/auth/permission-catalog.ts` is coarse-grained — `<ModuleCode>:<action>`, `action` ∈ `{view,create,edit,delete}` derived automatically from HTTP method + a small path-suffix override list, department-bucketed role templates (Owner/Sales Manager/Accountant/Supervisor). Adding a module is one `MODULES` array entry; `ALL_PERMISSIONS`/`ownerPermissions()` derive from it automatically, and the vitest `globalSetup` re-seeds it every run — no separate "permission string" system exists or should exist. The wiring pushed by the other commit invented a completely different, incompatible fine-grained taxonomy (`security:read`, `cache:manage`, …) that doesn't correspond to this real system at all.

- **M23 Security & Governance — MOUNTED at `/api/v1/security`.** Added `createPolicy`/`listPolicies` to `PolicyEngineService` and `createPolicy`/`listPolicies` to `DataRetentionService` (+ real Prisma-backed repository implementations) — the obvious missing write/list counterparts to their existing read methods, over already-migrated tables (`security_policy`, `data_retention_policy`); not new architecture. New `controllers/security.controller.ts` + `routes/security.routes.ts` (policy evaluate/list/create, retention list/create/execute). Added a `/execute$ -> edit` action override (same class as the existing `/rebuild$` one). 7 new real HTTP+DB tests: 401 without a token, real policy create/evaluate incl. default-deny for an unconfigured resource (never a fabricated ALLOW), tenant isolation on listing, retention execute honestly no-ops with no registered executor, input validation. `TenantIsolationGuard`/`AuthorizationService` remain library-only, as M23's own notes always said — not every M23 capability needed an HTTP shape.
- **M24 Performance & Cache — MOUNTED at `/api/v1/performance`.** Read-mostly surface over the existing `PerformanceMonitorService`/`CacheMetricsService`/`QueryOptimizer`/`IndexManager` singletons. Performance snapshots are tenant-scoped (verified: a sample recorded under another tenant never appears); cache-metrics/query-optimizer are genuinely process-wide (no tenant dimension in their own data model) so kept Owner-only, same tier as M19-monitoring. `indexManager.proposeIndex` never issues DDL. 6 new tests.
- **M28 Reports & Export — MOUNTED at `/api/v1/reports-export`** (deliberately not `/api/v1/reports` — M17 already owns that). Build+export endpoints over the real `ReportBuilder`(consumes M27's live metrics)/`ReportExportService`(real CSV, PDF/XLSX correctly unregistered). **Scheduling still not wired** — `ExportScheduler` has no real `SchedulerPort`; M13's job-creation methods aren't public, and M13's `scheduled_job` table requires a `ruleId` FK to an `AutomationRule` (`onDelete: Cascade`), so wiring M28's report schedules through it would mean inventing a matching automation rule per scheduled report and guessing how M13's rule executor would react to firing on it — flagged for an explicit owner decision, not guessed. 4 new tests, including proof a body-supplied `tenantId` is ignored.
- **M29 Mobile Auth & Push — MOUNTED at `/api/v1/mobile`.** `auth/login`/`auth/refresh` added to `app.ts`'s `PUBLIC_PREFIXES` (pre-auth, same treatment as `/api/v1/auth/login`); `push/send` goes through the normal full auth chain. 5 new tests incl. wrong-password 401 (not a stray 500) and honest not-delivered push (no real provider configured).
- **M31 Workforce & HR Intelligence — MOUNTED at `/api/v1/workforce`.** Talent-profile get/upsert (real `talent_profile` table + verified M12 `employee` check — rejects an unverified `employeeId` rather than creating an orphan profile), skill-match/skill-gap/role-fit (pure computation composed over a fetched real profile, never invented data), capacity-plan (pure computation). `WorkforceIntelligenceService`/`WorkforceForecastService`/etc. have no default singleton (no verified provider) and stay unexposed, same as M30. 7 new tests.
- **M30 AI/ML Guards & Reliability — deliberately left unmounted.** Its only real singletons (`retryService`, `timeoutService`, `idempotencyService`, `aiDataGuard`) are code-level utilities meant to wrap *other* code's async operations — there is no sensible client-facing HTTP shape for "retry an arbitrary operation" or "timeout an arbitrary operation" without fabricating a demo endpoint. The real fix for M30's reachability is other modules (e.g. M18's payment-gateway calls) importing and using these directly — that touches live financial call sites and needs careful, reviewed changes, not a rushed overnight route. Flagged for the owner, not invented.
- **M25 Real-Time & Events — LIVE at `/realtime` (socket.io), attempted and completed after the other five.** Added `socket.io`^4.8.3 (backend dependency) + `socket.io-client`^4.8.3 (dev, for the test suite) via `npm install --workspace=backend` — verified zero new vulnerabilities from either package (`npm audit` only shows a pre-existing, unrelated `prisma`/`deepmerge-ts` dev-tooling advisory). New `websocket/socketio-adapter.ts`: attaches to the *same* `http.Server` `server.ts`'s `app.listen()` already returns (no second server) at path `/realtime`; every connection goes through the real, already-tested `webSocketServer.handleConnection`/`webSocketAuthGuard` (same `authInternal.verifyAccessToken` as HTTP) — an unauthenticated or invalid-token socket gets an `auth_error` and is disconnected immediately, never left open; a valid socket is joined to `tenant:{tenantId}`/`user:{userId}` rooms derived *only* from the server-verified identity, never from anything the client sent. Bridged the one real cross-module event that maps onto M25's own `CLIENT_DELIVERABLE_EVENTS` allow-list: M16's real `notification.sent` (verified call site) now reaches connected clients of that event's tenant as `NOTIFICATION.SENT`, translated at the subscription boundary — neither M16's nor M25's existing event names were changed. Did not touch the REST-path collision from the earlier (non-functional) push since M25 was never given a REST layer here — it's WebSocket-only, so there's no `/api/v1/*` path to collide with M16 on. 5 new tests using a real `socket.io-client` against a real ephemeral `http.Server`: valid-token auth returns the server-verified identity, no-token and garbage-token both rejected-and-disconnected, tenant-isolated broadcast (a second tenant's socket never receives another tenant's message), and the real M16 event bridge delivering end-to-end.
- **M32/M33/M34 — still not extracted.** Their actual backend source exists only inside unextracted zip files at the repo root (`GNT_M23_to_M34_FINAL_COMPLETE.zip` covers M23-M32, `M33_M34_Modules.zip` covers M33/M34) — never unpacked into `backend/src/modules/`. Extracting + wiring each needs the same real-repo verification pass M23-M31 already got (checking every claimed dependency, dropping duplicates against M08/M09/M22 per M34's own notes) — a multi-hour job per module done properly, not attempted tonight given the remaining time budget after M23/24/28/29/31.

**Verified after all six mountings (M23/24/28/29/31/25):** `tsc --noEmit` clean (backend+frontend) after every change; each module's own suite green individually (M23 26/26, M24 16/16, M28 9/9, M29 12/12, M31 13/13, M25 13/13). Full `TEST_DB=1` suite (157 files) run and passed clean (157/157, 769/769) after the M23/24/28/29/31 batch. **After the M25 change, the full-suite re-run was attempted three times and killed each time by the environment running low on memory** (not a test failure — each attempt's log showed 0 failures up to the point it was killed, and system memory fully recovered within seconds each time, ruling out a real leak). Not claiming a clean full-suite result that didn't finish. Confidence for M25 specifically rests on: its own isolated suite (13/13, including a real live socket.io client against a real ephemeral server) passing outright, and the fact that its two production-code changes are structurally incapable of affecting the other 156 files — `server.ts` (where `attachRealtimeServer` is called) is the process entry point and is never imported by any test file (every test imports `app`/`registerModules` from `app.ts` directly, confirmed across the test suite), and `socket.io`/`socket.io-client` are purely additive dependencies touching no existing import graph. Every new permission-catalog `MODULES` entry (M23/24/28/29/31) defaults new modules to Owner-only access (`department: 'admin'`) — matching the existing M12/M17/M19 pattern — so no other role silently gained new access tonight; the owner should explicitly extend Sales Manager/Accountant/Supervisor templates if broader access is wanted later. M25 has no permission-catalog entry — WebSocket auth is enforced entirely by its own real token verification, independent of the Express permission middleware. **Owner should re-run the full suite once when memory allows, as a final sanity check on M25.**
