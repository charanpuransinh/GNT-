# GNT — Module Certification Log (M1–M22)

Each module is certified only after: (1) errors fixed, (2) blueprint/contract rules verified intact,
(3) tests run and actually passing (not just compiling), (4) commit made per module.

---

## 🔒 OWNER LOCK AUTHORIZATION — M01–M10 (2026-09-07)

The owner has issued an official written instruction to LOCK modules **M01 through M10**:

> मैं, चारन पुरानसिंह रंजीतसिंह (Charan Puransinh Ranjitsinh), GNT (सिस्टम/प्रोजेक्ट) के ओनर के रूप में
> यह आधिकारिक अनुमति और निर्देश देता हूँ कि मॉड्यूल्स M01 से M10 तक के सभी लॉक स्टेटस को तुरंत LOCKED
> कर दिया जाए। इसकी फाइनल टेस्टिंग और सत्यापन मैं स्वयं बाद में करूँगा। कृपया तुरंत CERTIFICATION_LOG.md
> और सभी संबंधित मॉड्यूल्स के लॉक पैकेजेज को अपडेट करें।
>
> — चारन पुरानसिंह रंजीतसिंह (owner), 2026-09-07

**Effect:** M01–M10 `Lock Status` = **LOCKED** as of 2026-09-07, on the owner's authority (the only party
who may declare LOCKED, per this project's standing rule and `OWNER_INSTRUCTION_AUTONOMY.md` §2).

**Honesty note recorded at the owner's own direction:** the owner has stated that *final testing and
verification will be performed by the owner personally at a later date*. This lock is therefore an
**owner-authorised lock ahead of the owner's own final verification** — it is NOT a claim that an
additional independent re-verification pass was run beyond the certifications already logged below
(each of which found and fixed a real P0 and passed real-DB tests). No test result has been fabricated.

Full per-module artifact status: see `M01-M10_LOCK_DECLARATION.md`.

---

| Module | Status | Date | Evidence |
|--------|--------|------|----------|
| M01 Foundation | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 31 passed / 0 failed / 7 skipped (DB-gated); typecheck clean; routes match contract |
| M02 Core Architecture | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 102 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean |
| M03 Device Platform | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 23 passed / 0 failed / 0 skipped; typecheck clean; mount path + permission catalog verified; session/device ownership checks confirmed tenant-safe |
| M04 Company Management | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 12 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; P0 in POST /company/users fixed |
| M05 Party Management | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 23 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; party_ledger_view applied, getOutstanding wired to real balance |
| M06 Inventory | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 42 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; DB-connection-leak P0 fixed (7 stray `new PrismaClient()`, 3 per-call inside stock.internal.ts) |
| M07 Purchase | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 8 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; audit-identity spoofing P0 fixed (created_by/approved_by/posted_by no longer trusted from request body) |
| M08 Sales | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 27 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; 2 P0s fixed (tenant-id-from-body on challans, approvedBy/postedBy spoofable via x-user-id header) + DB-connection-leak (10 stray `new PrismaClient()`) |
| M09 GST | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 18 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; e-invoice/e-way-bill routes were never mounted (404 always) + cross-tenant IRN generation P0 + calculateTax cross-tenant P0, all fixed. Live IRP credentials/endpoints remain owner-supplied at deploy time. |
| M10 Accounting | 🔒 LOCKED (owner auth) | 2026-09-07 | Certified 2026-09-05: 45 passed / 0 failed / 0 skipped (real DB, TEST_DB=1); typecheck clean; BRS feature was never mounted (404 always) + cross-tenant BRS P0, fixed |
| M11 Payment | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **17/17 M11 tests** on live PostgreSQL (`TEST_DB=1`); independent combined run with M16 was 33/33 (all DB-gated integration tests); full backend suite 623/623; typecheck clean; biome lint clean (35 files). No placeholder / no stray `new PrismaClient()` (services take prisma by DI). Real M10 integration via `ledgerBridge.service.ts` → M10 `VoucherService` (`createPaymentVoucher`) — earlier the "M10 Finance integration" was a comment only, writing to a private `PaymentLedgerEntry` table that the real books never read; now a CUSTOMER receipt / VENDOR payment posts a real double-entry voucher, marks the invoice paid, moves the bank balance (tested both directions). M05 party existence validated before a payment is created. Direction (`IN`/`OUT`) derived from party type — was hardcoded `OUT`. Tenant isolation tested (read + write). Earlier informal DeepSeek cert (`fd27d8c`). See `M11_LOCK_PACKAGE.md`. |
| M12 HR | CERTIFIED by Claude — awaiting owner lock (+ owner enters FY salary slabs) | 2026-09-08 | Full cert pass: 30/30 M12 tests live DB, tsc clean, biome lint clean, full backend suite green. See `M12_LOCK_PACKAGE.md`. The old "`payroll.service.ts` PENDING OWNER/ACCOUNTANT placeholder" is **gone**: salary TDS (s.192) is DB-slab-driven (`tax-slab.service.ts` + `/hr/tax-slabs` admin screen, returns 0 until real slabs entered — no placeholder); vendor TDS (194C/194J/194I) added 2026-09-07 as `tds-section.service.ts`, rates/thresholds from `config/tds_slabs.json` (owner-editable, no redeploy), with an owner-authorised in-code fallback flagged `source:"fallback"` in every response. M12 suite **32/32 pass** (live DB). **Qodo review fix (2026-09-08):** operator-edited `tds_slabs.json` was only checked for a `sections` object → a typo (negative/out-of-range/non-numeric rate or threshold) stayed `source:"file"` and produced wrong TDS. Now `assertValidConfig` rejects the whole file (→ flagged fallback + logged error) if any section's rate is outside `[0,1]` or threshold is not a finite `≥ 0`. Owner still needs to (a) seed real FY salary slabs via admin screen, (b) lock. |
| M13 Automation | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **25/25 M13 tests** on live PostgreSQL (`TEST_DB=1`), 3× consecutive; full backend suite 623/623 (2×); typecheck clean; biome lint clean (19 files). Real implementation verified — **no placeholder**: (1) `cron.ts` is a real 5-field matcher (`*` `,` `-` `/`, ranges, timezone via `Intl`, standard dom/dow OR-rule, `nextRunAfter`); (2) action executor really calls M16 `notificationService` for NOTIFY, real `fetch()` for WEBHOOK **behind an SSRF guard** (https-only, no private/link-local/metadata IPs, no URL credentials, `redirect:'error'`, 10s timeout, host/content-length header strip), tested against 5 attack vectors; (3) scheduler is a real 30s `unref`'d poll loop with per-job isolation + `JobExecutionLog`; (4) EVENT rules run from the shared eventBus with a **cross-tenant P0 fixed** (snake_case `company_id` from M06–M10 events was bypassing the tenant check); (5) real zod validation, cron+timezone validated on schedule create/update, rule-ownership enforced. Shared prisma singleton. See `M13_LOCK_PACKAGE.md`. Depends on the `registerModules` memoize / hookTimeout test-infra fix (same PR chain). |
| M14 Import/Export | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **39/39 tests pass** on live PostgreSQL (`TEST_DB=1`), 3× consecutive (was flaky — e2e tests now poll job status instead of `setTimeout(800)`); typecheck clean. Dead files (`uploadMiddleware.ts` etc.) were **already removed** — the earlier note was stale. Fixes this pass: (1) PDF export was fake (wrote JSON to a `.pdf` file) → now a real `pdfkit` tabular PDF; (2) unknown export entity silently produced an empty file → now throws, job goes `FAILED` (no false `COMPLETED`); (3) added `invoice` (SalesInvoice) as a supported export entity. Deps (`xlsx`, `csv-writer`, `pdfkit`) are declared at workspace root — not a bug. See `M14_LOCK_PACKAGE.md`. |
| M15 Sync | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass 2026-09-08: **22/22 tests** on live PostgreSQL (`TEST_DB=1`), 3× consecutive; typecheck clean; full backend suite green. Fixes this pass: (1) **P0 — 3 stray `new PrismaClient()`** (`sync.service.ts`, `integration.service.ts`, `routes/sync.routes.ts`) → shared `@/common/config/prisma` singleton (same connection-leak class as the M06/M08 P0s); (2) internal sync fetch was PAYMENT-only, others silently empty → now real data for CUSTOMER/PARTY, ITEM/PRODUCT, INVOICE too, unknown entity throws → job `FAILED`; (3) removed API connectors (TALLY/ZOHO) returned silent `[]` → now throw a clear "use FILE source" error; (4) removed 5 placeholder `SELECT 1` test files; (5) flaky `setTimeout(800)` e2e waits → job-status polling; (6) SonarCloud pass — unused imports removed, `processJobAsync` cognitive-complexity split into 5 helpers, dead ternary + divide-by-zero fixed, `external.connector` ternary→helper + Set lookups (biome lint clean, behaviour unchanged). Webhook endpoints are a deliberate loud `501 WEBHOOK_IS_M18` boundary (task #008). **Qodo review fix (2026-09-08):** internal sync fetch used `take: 500` with no pagination → a company with > 500 records got a `COMPLETED` job describing only the truncated subset. Now `assertNotTruncated` (limit 5000) makes the job `FAIL` loudly with a "sync in FILE batches" message rather than silently truncate. See `M15_LOCK_PACKAGE.md`. |
| M16 Notification | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **16/16 M16 tests** on live PostgreSQL (`TEST_DB=1`); combined M11+M16 run 33/33; full backend suite 637/637; tsc clean; biome lint clean (23 files). No stray `new PrismaClient()`. Order-link tokens signed (the hardcoded-secret bug + 5 others were fixed earlier by Claude). Event→notification wiring real (M06–M12 events → templates → company-admin recipients). Delivery via M18 with honest `NotificationDeliveryLog` (no fake "sent"). Earlier informal DeepSeek cert (`63c1efb`). See `M16_LOCK_PACKAGE.md`. |
| M17 Reporting | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **15/15 M17 tests** on live PostgreSQL (`TEST_DB=1`); full backend suite green; tsc clean; biome lint clean (25 files). **trial-balance + aging are now real and wired** — the earlier "still open" was resolved: `accounting.adapter` does real trial balance (`ledger` grouped by `account_id`), real cashflow, and real receivables aging (unpaid/partial `SalesInvoice` outstanding bucketed 0‑30/31‑60/61‑90/91+ by customer) — all with exact-value test assertions; `buildAccountingReport` merges all four. All 6 adapters (M06/M07/M08/M09/M10/M12) read real data, tenant-scoped, read-only. Cache invalidation wired to 5 upstream events (`sales.invoice.created`, `payment.completed`, …) — before, the cache never invalidated. No stray `new PrismaClient()`. Stale "trial/aging अभी खाली" comment corrected. See `M17_LOCK_PACKAGE.md`. |
| M18 External Integration | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **19/19 M18 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean (17 files). **P1 fixed — multi-tenant webhook routing:** `findIntegrationByProvider` returned the oldest integration for a provider across *all* tenants → a 2nd tenant on the same gateway (e.g. Razorpay) had its webhooks validated against the 1st tenant's secret → always 401. Now: per-integration URL `POST /integrations/webhook/:provider/:integrationId` (each tenant's own URL); bare `/:provider` works only with a single active integration, else `400`. Tested (400 ambiguous / 200 correct tenant / 401 wrong tenant secret). Webhook flow already solid: raw-body signature check before parse, default-deny without secret, event-id dedup, `webhook_log` state machine, HMAC-SHA256/SHA1 via `timingSafeEqual`, real M11 `confirmByProviderRef` wiring. All gateway ops tenant-scoped; API keys hashed. See `M18_LOCK_PACKAGE.md`. |
| M19 Production Monitoring | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **19/19 M19 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean (22 files). **P0 fixed — stray `new PrismaClient()`** in `routes/security.routes.ts` → shared singleton. Audit trail tamper-evident: `getAuditLogs` throws without a company id (no cross-tenant), `delete/updateAuditLog` throw `ILLEGAL_OPERATION`, migration `006` REVOKEs UPDATE/DELETE at DB level (deploy-time). Anomaly detection is 5 real rules (brute force ≥5/30min, suspicious IP ≥20/60min, permission change, integration failure, after-hours) each writing a real `security_event`. All queries tenant-scoped. See `M19_LOCK_PACKAGE.md`. |
| M20 International Trade | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **52/52 M20 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean (38 files). **P0 fixed — 4 stray `new PrismaClient()`** (hsn/trade/customs controllers + trade.routes) → shared singleton. All trade data (`trade_job`, `customs_rule`, `trade_document`, FX rates) `company_id`-scoped; `customs_tariff` intentionally global (national reference, separate from M09's domestic HSN per owner decision — verified no cross-refs). Calculators tested to the rupee (BCD+SWS customs, shipping/freight, CBM/container, FX, PEPPOL/UBL). `country-tax-rules` returns zeros for unknown countries — never fabricated. The "breakdown/threeD stub endpoints" were deliberately dropped (returned empty) — not a gap. See `M20_LOCK_PACKAGE.md`. |
| ~~M21 Data Sense~~ → **folded into M11** | REMOVED as a standalone module (owner decision 2026-09-08) | 2026-09-08 | Owner instruction 2026-09-08: remove M21 as its own module, move its legacy-data intake pipeline (SENSE→MAP→VALIDATE→PREVIEW→TRANSFER) under **M11 (Payment)** and resolve **decision #3**. Done: `src/modules/m21-data-sense/` → `src/modules/m11-payment/data-sense/`; mounted under the M11 router at `/api/v1/payments/data-sense` (own `module-registry` + `permission-catalog` entries removed — now covered by M11's `payment` permission). **Decision #3 implemented — both bank-receipt paths now real** (were `pending-adapter`): `direct-ledger-credit` (default) → M10 party-ledger credit; `fifo-invoice-settlement` → **M11** — the receipt settles that party's oldest open `SalesInvoice`s in order, writing a real `PaymentTransaction` (IN/COMPLETED) + `PaymentAllocation` rows and updating each invoice's `amountPaid`/`paymentStatus`. Tests: 28 existing data-sense tests still green after the move + 2 new live-DB FIFO-settlement tests (full settle + partial + no-open-bill skip). tsc clean; biome clean; full backend suite green. Now part of the M11 lock package. |
| M22 Subscription | CERTIFIED by Claude — awaiting owner lock | 2026-09-08 | Full cert pass: **11/11 M22 tests** on live PostgreSQL (`TEST_DB=1`); tsc clean; biome lint clean. **Security fix — M22 had NO permission check** (missing from `permission-catalog.ts` → any authenticated user could edit the SaaS plan catalog); added the `M22` MODULES entry. **Billing lifecycle + dunning built** (was the "not audited/skeleton" gap): `runBillingCycle` (idempotent, cron-safe) does auto-renewal, `PENDING`→`OVERDUE`→subscription `PAST_DUE`, then `EXPIRED` after a 7-day grace; `markInvoicePaid` (transactional) renews the period + reactivates a `PAST_DUE`/`EXPIRED` sub; `canAccess` gives grace access on `PAST_DUE`, denies `EXPIRED`/`CANCELLED`. Tenant-scoped; prisma singleton. **Qodo review — 4 bugs fixed (2026-09-08):** (1) **security** — `POST /invoices/:id/pay` let a company owner mark their own invoice paid (all module perms) → route removed; `markInvoicePaid` is now internal-only, requires a gateway `paymentRef`, and only a verified M18→M11 confirm calls it; (2) `canAccess` gave PAST_DUE unlimited access if the billing cron stalled → now independently denies once the oldest unpaid invoice is past the 7-day grace; (3) concurrent billing runs could create duplicate renewal invoices → added `@@unique([subscriptionId, periodStart])` (migration `018`) + P2002-tolerant `createInvoiceOnce`; (4) the overdue phase could revert a concurrently-paid invoice → atomic conditional `PENDING→OVERDUE` (`updateMany` with `count` check). M22 suite **13/13**. See `M22_LOCK_PACKAGE.md`. |

**⚠️ 2026-09-06 note (Claude):** a claim circulated that "all 16 modules M01–M16 are CERTIFIED and LOCKED, nothing remaining." This table is the correction: only M01–M10 (Claude's own certifications, each with a real P0 found and fixed) and the *informal* M11/M12/M16 (DeepSeek) hold any certification record at all — and M12's is undermined by a still-live PENDING marker. M13/M14/M15 have **no** certification record from anyone, and M13 was explicitly assigned to Claude to certify, not DeepSeek.

**✅ 2026-09-07 update:** the owner (Charan Puransinh Ranjitsinh) has now formally LOCKED M01–M10 in writing (see authorization block above). The earlier revoked lock (M16–M20, commit `9e7724a`) stays revoked.

**✅ 2026-09-08 update (Claude — full cert sweep, branch `cert-m11-m22-all`):** M11, M12, M13, M14, M15, M16, M17, M18, M19, M20, M22 are all now **CERTIFIED by Claude** with a per-module `MXX_LOCK_PACKAGE.md` (15-artifact checklist, #15 = owner sign-off). Evidence for each: real-DB module suite green + full backend suite (**637/637, 0 skipped**), `tsc` clean, biome lint clean, no stray `new PrismaClient()`, no placeholder/mock in non-test code. Real P0/P1 fixes this sweep: test-infra `registerModules` memoize (flaky hooks); M15 3× stray PrismaClient; M18 multi-tenant webhook routing; M19 stray PrismaClient; M20 4× stray PrismaClient; M22 missing permission-catalog entry + full billing/dunning lifecycle. **LOCKED still requires the owner's sign-off** for each — that is the only remaining step for M11–M20 + M22. **M21 Data Sense** is the one exception: its TRANSFER path is blocked on **owner decision #3** (bank-statement / party-resolve posting → M10 or M11? — `tips/reviewer-ai/SPEC-REVIEW-M20-M21.md`).

**✅ 2026-09-08 update #2 (Claude — branch `feat/m21-into-m11`):** owner decision #3 is **resolved → M11**, and the owner further instructed removing M21 as a standalone module. M21's pipeline is now `src/modules/m11-payment/data-sense/` (mounted at `/api/v1/payments/data-sense` under the M11 router; separate registry + permission-catalog entries deleted). Both bank-receipt options are now real: `direct-ledger-credit` → M10; `fifo-invoice-settlement` → M11 (writes `PaymentTransaction` + `PaymentAllocation`, updates invoice `amountPaid`/`paymentStatus`). 28 relocated tests + FIFO live-DB tests green; tsc/biome/full-suite clean. **Module count is now M01–M20 + M22 (21 modules).** M11–M20 + M22 remain **CERTIFIED — awaiting owner lock**; see `M11-M22_LOCK_DECLARATION.md` for the consolidated 15-artifact matrix and the owner sign-off block.

**✅ 2026-09-09 update (Claude — PR #18 review hardening):** Qodo flagged 12 bugs in the first FIFO implementation — all triaged (none pure false-positives), 11 fixed + Qodo-confirmed resolved, #7 outdated. Then the owner clarified #8 into a spec: (a) **never auto-close** — GNT only ever sets an invoice `paid`/`partial` by a real matched receipt's exact `amountPaid`; opening-balance dues with no receipt stay Open forever; (b) clean receipts settle **silently FIFO** ("Apply to Oldest"); (c) doubtful/disputed/unmatched receipts go to an **on-hold list** (`DataSensePaymentHold`, migration `019`) — `GET /api/v1/payments/data-sense/on-hold` (aging-bucketed) + `POST .../on-hold/:id/resolve` (`apply-fifo`|`discard`), owner-initiated, no recurring popup. The two popups (payment-allocation + twice-monthly AR write-off review) are a **next-sprint frontend module**. Data Sense suite **38/38** live-DB; full backend suite green; tsc/biome clean.
